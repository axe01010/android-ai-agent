# --- runtime dependencies ---------------------------------------------------
# PyYAML is only needed if you use config.yaml (the default). The JSON config
# path needs nothing extra.
PyYAML>=6.0; python_version >= "3.8"